# Reusable workflows

Evaluation: prefer repo-local workflows with a pinned Rust toolchain until a shared composite action is justified.
Reusable candidates later: markdownlint, draft-skip pattern, toolchain pin.
